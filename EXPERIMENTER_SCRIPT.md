# 実験者用台本

## 0. 実験前の準備

実験者は、参加者が来る前に以下を確認する。

- 実験用ページがブラウザで開けること
- マイクが接続されていること
- ブラウザでマイク権限を許可できること
- 録音テストが可能な静かな環境であること
- 参加者の `Subject ID` が準備されていること

参加者が着席したら、画面が見やすい位置にあるか、マイクに声が入りやすいかを確認する。

## 1. 開始時の説明

実験者：

> 本日は実験にご協力いただきありがとうございます。  
> この実験では、画面に表示される単語を声に出して読んでいただきます。  
> そのあと、その単語の意味や使い方を口頭で答えていただきます。  
> 最後に、その単語について短い質問に回答します。

> 操作は画面の説明に沿って進めます。

## 2. 参加者情報とマイク準備

実験者は、準備画面で `Subject ID` を入力する。

実験者：

> まず、参加者番号を入力します。  
> マイク権限は画面を開いたときに確認されます。

ブラウザの確認ダイアログが出た場合は許可する。マイク状態が準備完了にならない場合は、`マイク権限を再確認` を押す。

マイクが複数ある場合は、使用するマイクを選択する。

実験者：

> マイクの準備ができました。これから画面の説明に進みます。

実験者は `説明へ進む` を押す。

## 3. 操作説明

説明画面は複数ページに分かれている。実験者は画面上の説明と同じ内容を読み上げる。画面にない補足説明は、参加者から質問があった場合に限って行う。

### 3.1 単語の読み上げ

実験者：

> カウントダウンのあと、画面中央に単語が大きく表示されます。  
> 表示された単語を、できるだけ自然な速度で声に出して読んでください。  
> 読み終わったら、すぐに `Space` キー、または画面のボタンを押してください。

実験者は `次へ` を押す。

### 3.2 意味や使い方の口頭回答

実験者：

> 読み上げが終わると、同じ単語について答える画面に進みます。  
> 意味を説明しても、例文を作ってもかまいません。  
> わかる範囲で答えてください。答え終わったら `Space` キー、または終了ボタンを押してください。

実験者は `次へ` を押す。

### 3.3 事後質問

実験者：

> 読み上げと口頭での回答が終わったあと、その単語について短い質問に答えます。  
> 質問は一つずつ表示されます。  
> 数字を選ぶと次の質問に進みます。

実験者は `次へ` を押す。

### 3.4 練習の案内

実験者：

> 操作に慣れるため、最初に2回の練習があります。  
> 練習が終わると、本番に進みます。  
> 準備ができたら、練習を開始してください。

参加者が準備できたことを確認し、実験者は `練習を開始する` を押す。

## 4. 練習

練習では、実験本番と同じ流れで操作を行う。

各単語について、以下の順で進む。

1. カウントダウンを見る
2. 表示された単語を声に出して読む
3. 読み終わったら `Space` キー、または `次へ進む` を押す
4. 意味や使い方について口頭で答える
5. 答え終わったら `Space` キー、または `終了` を押す
6. 事後質問に回答する

練習中に参加者が迷った場合、実験者は必要最小限の操作説明を行う。

実験者：

> 今の練習は操作に慣れるためのものです。  
> 本番でも同じ流れで進みます。

練習が終わったら、画面に練習完了メッセージが表示される。

## 5. 本番前の確認

実験者：

> 練習は以上です。  
> ここから本番に入ります。  
> 準備はよろしいですか。

参加者の準備ができたら、実験者は `本番を開始する` を押す。

## 6. 本番

本番では、実験者は基本的に介入せず、参加者が画面の指示に従って進めるのを見守る。

各 trial の流れは以下の通り。

1. カウントダウンが表示される
2. 単語が表示される
3. 参加者が単語を読み上げる
4. 参加者が `Space` キー、または `次へ進む` を押す
5. 参加者が `回答を録音する` を押す
6. 参加者が意味や使い方について答える
7. 参加者が `Space` キー、または `終了` を押す
8. 参加者が事後質問に回答する
9. 次の trial に進む

参加者が質問した場合は、実験内容に影響しない範囲で操作方法のみ説明する。

例：

> わかる範囲で答えていただければ大丈夫です。  
> 意味を説明しても、例文を作ってもかまいません。

> 読み終わったら `Space` キー、または画面のボタンを押してください。

> 数字を選ぶと次の質問に進みます。前に戻りたい場合は `戻る` を押してください。

## 7. 実験終了後

すべての trial が終わると、完了画面が表示される。

実験者：

> これで実験は終了です。  
> ご協力ありがとうございました。  
> 最後に、こちらでデータを保存しますので、少しお待ちください。

## 8. データ保存

実験者は完了画面で以下を実行する。

1. `データ一式をZIPでダウンロード` を押す
2. ダウンロードされた ZIP ファイルを確認する
3. 必要に応じて、`results.json のみ`、`CSV のみ`、または `すべての音声をダウンロード` をバックアップとして使用する

ZIP ファイルには以下が含まれる。

- 結果 JSON ファイル
- trial ごとの CSV ファイル
- 読み上げ音声
- 意味や使い方についての口頭回答音声

実験者は、`Subject ID` と保存ファイル名が対応しているか確認する。

## 9. 終了挨拶

実験者：

> データの保存が確認できました。  
> 本日の実験は以上です。  
> ご協力いただき、ありがとうございました。

必要に応じて、参加者に退出案内を行う。

## 10. トラブル時の対応

### マイク権限が許可できない場合

実験者：

> ブラウザのマイク権限を確認します。少しお待ちください。

ブラウザ設定を確認し、必要に応じてページを再読み込みしてから再度 `マイク権限を再確認` を押す。

### 録音中に参加者が間違えた場合

実験者：

> そのまま続けてください。  
> わかる範囲で答えていただければ大丈夫です。

基本的には trial を途中でやり直さず、実験者メモに状況を記録する。

### 操作が止まった場合

実験者：

> 画面のボタンを押して進めてください。  
> 読み上げや回答が終わっている場合は、`Space` キーでも進めます。

### ページを誤って更新した場合

ページを更新すると、その時点までのブラウザ内データが失われる可能性がある。実験者は状況を記録し、必要に応じて実験を最初からやり直す。
